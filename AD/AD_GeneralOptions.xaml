<Window x:Class="HoopsFast.AD.AD_GeneralOptions"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HoopsFast.AD"
        mc:Ignorable="d"
        Title="AD_GeneralOptions" Height="450" Width="800"
        Icon="pack://application:,,,/Images/TS3D-Fav.ico">
    
    <Grid>
        <StackPanel Margin="10">
            <Label FontWeight="Bold">General Options</Label>

            <CheckBox x:Name="chkEcho">Echo</CheckBox>
            <DockPanel>
                <Label Content="DTAero:"/>
                <TextBox Text="{Binding Value, StringFormat=N2}"/>
            </DockPanel>

            <DockPanel>
                <Label Content="WakeMod:"/>
                <TextBox Text="{Binding Value, StringFormat=N2}"/>
            </DockPanel>

            <DockPanel>
                <Label Content="AFAeroMod:"/>
                <TextBox Text="{Binding Value, StringFormat=N2}"/>
            </DockPanel>
        </StackPanel>

    </Grid>
</Window>
