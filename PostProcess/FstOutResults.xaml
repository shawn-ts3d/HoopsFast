<Window x:Class="HoopsFast.PostProcess.FstOutResults"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HoopsFast.PostProcess"
        
        mc:Ignorable="d"
        Title="FstOutResults" Height="477" Width="800"
        Icon="pack://application:,,,/Images/TS3D-Fav.ico">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button x:Name="btnPlot" Content="Plot" HorizontalAlignment="Left" Click="btnPlot_Click" Height="33" Width="90" Grid.Row="0" Margin="10" />

        <DataGrid HorizontalAlignment="Left" Margin="12,0,0,0" x:Name="dataGridOutResults" VerticalAlignment="Top" Grid.Row="1">
            <!--<DataGrid.Columns>
                <DataGridCheckBoxColumn Header="Selected" Binding="{Binding selected}" />
                <DataGridTextColumn Header="Name" Binding="{Binding name}"/>
                <DataGridTextColumn Header="Unit" Binding="{Binding unit}"/>
                <DataGridTextColumn Header="Description" Binding="{Binding description}"/>
            </DataGrid.Columns>-->
        </DataGrid>

    </Grid>
</Window>
