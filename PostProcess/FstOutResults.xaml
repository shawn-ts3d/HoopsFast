<Window x:Class="HoopsFast.PostProcess.FstOutResults"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HoopsFast.PostProcess"
        
        mc:Ignorable="d"
        Title="FstOutResults" Height="477" Width="800">
    <Grid>

        <DataGrid Height="384" HorizontalAlignment="Left" Margin="12,21,0,0" x:Name="dataGridOutResults" VerticalAlignment="Top">
            <!--<DataGrid.Columns>
                <DataGridCheckBoxColumn Header="Selected" Binding="{Binding selected}" />
                <DataGridTextColumn Header="Name" Binding="{Binding name}"/>
                <DataGridTextColumn Header="Unit" Binding="{Binding unit}"/>
                <DataGridTextColumn Header="Description" Binding="{Binding description}"/>
            </DataGrid.Columns>-->
        </DataGrid>

        <Button x:Name="btnPlot" Content="Plot" HorizontalAlignment="Left" Margin="617,32,0,0" VerticalAlignment="Top" RenderTransformOrigin="16.447,-18.681" Height="33" Width="90" Click="btnPlot_Click"/>

        


    </Grid>
</Window>
